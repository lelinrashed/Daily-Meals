import 'package:flutter/material.dart';

class FavouritsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Favourit screen'),
    );
  }
}
